# JavaScript Amazon Project

![Amazon Project Preview](images/Screenshot.png)

โปรเจกต์เว็บแอปพลิเคชันอีคอมเมิร์ซที่จำลองรูปแบบมาจาก Amazon โดยเน้นการฝึกฝนการเขียน JavaScript แบบ Modular และการจัดการข้อมูลแบบ Dynamic

## 🚀 คุณสมบัติเด่น (Features)

- **Product Grid**: แสดงรายการสินค้าแบบ Dynamic โดยดึงข้อมูลจากไฟล์ JSON ผ่าน Fetch API
- **Interactive Cart**: ระบบตะกร้าสินค้าที่สามารถเพิ่มสินค้า, ปรับจำนวน, และลบสินค้าได้แบบเรียลไทม์
- **Persistent Storage**: บันทึกข้อมูลตะกร้าสินค้าไว้ใน `localStorage` ทำให้ข้อมูลไม่หายไปเมื่อโหลดหน้าเว็บใหม่
- **Checkout System**: หน้าตรวจสอบรายการสั่งซื้อที่มีการคำนวณราคาสินค้า, ค่าจัดส่ง, และภาษีโดยอัตโนมัติ
- **Delivery Options**: สามารถเลือกวันจัดส่งสินค้าได้หลากหลายรูปแบบ โดยใช้ `dayjs` ในการคำนวณวันที่ล่วงหน้า
- **Responsive Design**: ปรับแต่งหน้าตาเว็บไซต์ให้เหมาะสมกับการใช้งานทั้งบน Desktop และ Mobile

## 🛠️ เทคโนโลยีที่ใช้ (Tech Stack)

- **HTML5**: โครงสร้างของเว็บไซต์
- **CSS3 (Vanilla)**: การตกแต่งเว็บไซต์แบบ Modular แบ่งไฟล์ตามความรับผิดชอบ
- **JavaScript (ES6 Modules)**: การจัดการตรรกะเบื้องหลังและการแบ่งโมดูลเพื่อให้อ่านง่ายและบำรุงรักษาง่าย
- **Fetch API**: สำหรับการดึงข้อมูลสินค้าจาก Backend (หรือไฟล์จำลอง)
- **LocalStorage API**: สำหรับการเก็บข้อมูลตะกร้าสินค้า
- **Day.js**: ไลบรารีสำหรับจัดการเรื่องวันที่จัดส่งและการคำนวณเวลา

## 📂 โครงสร้างโปรเจกต์ (Project Structure)

- `amazon.html`: หน้าหลักสำหรับแสดงรายการสินค้า
- `checkout.html`: หน้าสรุปรายการสั่งซื้อและชำระเงิน
- `orders.html` & `tracking.html`: หน้าประวัติการสั่งซื้อและการติดตามสถานะ (Static Mockup)
- `data/`: เก็บโมดูลจัดการข้อมูล เช่น `cart.js`, `products.js`, `deliveryOptions.js`
- `script/`: เก็บตรรกะการทำงานหลัก แบ่งเป็นโมดูลย่อยใน `checkout/` และ `utils/`
- `styles/`: เก็บไฟล์ CSS ที่แบ่งตามหน้า (Pages) และส่วนที่แชร์กัน (Shared)
- `images/`: ไฟล์สื่อต่างๆ ของโปรเจกต์

## 💻 วิธีการเปิดใช้งาน (How to Run)

1. ดาวน์โหลดหรือคัดลอกไฟล์โปรเจกต์ทั้งหมด
2. เนื่องจากโปรเจกต์นี้ใช้ **JavaScript ES Modules** คุณจำเป็นต้องเปิดผ่าน **Local Server** เท่านั้น
   - **แนะนำ**: ใช้ส่วนเสริม **Live Server** ใน VS Code
3. เปิดไฟล์ `amazon.html` ผ่าน Browser ของคุณ

---
สร้างสรรค์โดยเป็นส่วนหนึ่งของการเรียนรู้การพัฒนาเว็บด้วย JavaScript
